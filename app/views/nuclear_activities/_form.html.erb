<%= form_for(@nuclear_activity) do |f| %>

<%= error_explanation_for @nuclear_activity %>

  <div class="field">
    <%= f.label :statement %><br />
    <%= f.text_area :statement %>
  </div>
  <div class="field">
    <%= f.label :learning_outcome_id %><br />
    <%= f.number_field :learning_outcome_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
