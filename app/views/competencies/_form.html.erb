<%= form_for(@competency) do |f| %>

<%= error_explanation_for @competency %>

  <div class="field">
    <%= f.label :statement %><br />
    <%= f.text_area :statement %>
  </div>
  <div class="field">
    <%= f.label :level_description_id %><br />
    <%= f.number_field :level_description_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
